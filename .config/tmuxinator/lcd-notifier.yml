# ~/.tmuxinator/lcd-notifier.yml

name: lcd-notifier
root: ~/Developer/go/src/github.com/nicholasjackson/lcd-notifier

windows:
  - editor:
      panes:
        - nvim .
  - terminal:
      layout: even-horizontal
      panes:
        - zsh:
        - zsh:
