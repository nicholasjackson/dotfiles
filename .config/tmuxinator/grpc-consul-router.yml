# ~/.tmuxinator/grpc-consul-resolver

name: grpc-consul-resolver
root: ~/Developer/go/src/github.com/nicholasjackson/grpc-consul-resolver

windows:
  - editor:
      panes:
        - nvim .
  - terminal:
      layout: even-horizontal
      panes:
        - zsh:
        - zsh:
