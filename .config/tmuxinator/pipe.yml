# ~/.tmuxinator/pipe.yml

name: pipe
root: ~/Developer/go/src/github.com/nicholasjackson/pipe

windows:
  - editor:
      panes:
        - nvim .
  - terminal:
      layout: even-horizontal
      panes:
        - zsh:
        - zsh:
