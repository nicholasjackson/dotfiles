# ~/.tmuxinator/vault-spiffe.yml

name: vault-spiffe
root: ~/Developer/go/src/github.com/hashicorp/vault-plugin-spiffe-auth

windows:
  - editor:
      panes:
        - nvim .
  - terminal:
      layout: even-horizontal
      panes:
        - zsh:
        - zsh:
